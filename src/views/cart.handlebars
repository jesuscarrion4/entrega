<div class="container">
    <h1>Tu Carrito</h1>

    <ul class="cart-list">
        {{#each products}}
        <li class="cart-item">
            <p><strong>cafe:</strong> {{this.product.title}}</p>
            <p><strong>Cantidad:</strong> {{this.quantity}}</p>
        </li>
        {{else}}
        <li class="cart-item">El carrito no tiene contenido.</li>
        {{/each}}
    </ul>
</div>
